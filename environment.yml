name: temporal-causality-engine
channels:
  - conda-forge
dependencies:
  - python>=3.10
  - numpy
  - pandas
  - scipy
  - statsmodels
  - networkx
  - pip
  - pip:
      - tigramite
      - streamlit
      - pytest
